## Plan
1. Update `components/dashboard/OpportunityChart.tsx` to move the stacked bars (`value2` + `value3`) above the orange bar (`value1`) so the visual order matches your request.
2. Run project validators (lint/typecheck/tests) based on repo scripts and fix any issues if they appear.

If this looks good, confirm and I will implement.