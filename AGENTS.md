# AGENTS

Trả lời bằng tiếng việt 

## Excel cell dictionary

The detailed cell-by-cell dictionary for the Excel template lives at:

`public/hop-dong-doanh-thu-dictionary.json`

Use this JSON to map each cell (e.g., `A1`) to its role, data type, and input status when updating numbers without changing the template format.
